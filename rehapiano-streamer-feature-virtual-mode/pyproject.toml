[project]
name = "rehapiano-host"
version = "0.0.1"
description = "Host aplikácia pre RehaPiano zariadenia (UART->Python)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["pyserial-asyncio>=0.6"]

[tool.pyright]
pythonVersion = "3.11"
